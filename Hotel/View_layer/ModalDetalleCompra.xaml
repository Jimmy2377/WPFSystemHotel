<Window x:Class="Hotel.View_layer.ModalDetalleCompra"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel.View_layer"
        mc:Ignorable="d"
        Title="ModalDetalleCompra" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="tablaDetalleCompra" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Producto" Binding="{Binding ID_Producto}" />
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" />
                <!-- Agrega más columnas según tus necesidades -->
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Cerrar" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10"
                Width="100" Click="Cerrar_Click" />
    </Grid>
</Window>
